[project]
name = "hw1-2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "lightgbm",
    "networkx",
    "numpy",
    "pandas",
    "scikit-learn",
    "scipy",
    "tqdm",
]
